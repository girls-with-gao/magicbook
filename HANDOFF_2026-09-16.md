# 그림이야기 집 컴퓨터 작업 인수인계

이 문서는 집 컴퓨터에서 프로젝트를 바로 이어하기 위한 안내서다.

## GitHub 저장소

- <https://github.com/girls-with-gao/magicbook>
- 기본 브랜치: `main`

## 집에서 Codex에게 처음 보낼 말

> README.md, HANDOFF_2026-09-16.md, docs/superpowers/specs/2026-09-16-drawtale-bilingual-mvp-design.md를 먼저 모두 읽어줘. npm test와 npm run build로 현재 상태를 확인한 뒤, 휴대폰 화면으로 5단계 흐름을 확인해줘. 기존 파일은 말없이 덮어쓰거나 지우지 마.

## 현재 구현된 것

- Next.js 16.3.3 App Router로 이전 완료
- 그림일기 JPG·PNG·WebP 업로드
- 8MB 용량 제한과 서버 안전 검사
- 별명·나이·개인정보 확인
- AI 그림·손글씨 분석과 부모 수정 화면
- 한국어, 영어, 이중언어 보기 선택
- 원본 그림을 활용한 4페이지 이야기
- 페이지당 최대 2개의 한국어·영어 단어
- 브라우저의 문장 듣기 기능
- 열린 질문으로 종이 그림 활동에 복귀하는 종료 화면
- API 키가 없을 때 작동하는 발표용 예제 모드
- Vitest 자동검사

## 주요 파일

- `app/page.tsx`: 첫 화면
- `components/story-studio.tsx`: 5단계 전체 화면과 상태
- `app/api/analyze/route.ts`: 그림·손글씨 분석 통로
- `app/api/story/route.ts`: 4페이지 이중언어 이야기 생성 통로
- `lib/ai-prompts.ts`: AI 안전·형식 지침
- `lib/demo-data.ts`: API 키 없이 시연하는 예제
- `lib/image-validation.ts`: 이미지 형식·용량 검사
- `app/globals.css`: 어린이 터치 화면 디자인
- `tests/`: 이야기 형식·파일 검사·화면 이동 자동검사

`server.js`, `public/index.html`, `public/app.js`, `public/styles.css`는 최초 프로토타입 참고용으로 남겨두었다. 현재 `npm run dev`는 `app/`의 Next.js 웹앱을 실행한다.

## 집 컴퓨터에서 실행

준비물: Node.js 20.9 이상

```bash
npm install
npm run dev
```

인터넷 창에서 <http://localhost:3000>을 연다. 실제 AI 키가 없으면 `예제로 시작하기`를 누르면 된다.

## 실제 AI 키 사용

`.env.example`을 보고 `.env.local`을 만든 뒤 다음을 넣는다.

```env
OPENAI_API_KEY=본인의_키
```

이 값은 GitHub나 채팅방에 올리지 않는다. Vercel에서는 프로젝트의 Environment Variables에 같은 이름으로 넣는다.

## 자동 확인

```bash
npm test
npm run build
```

둘 다 오류 없이 끝나야 한다.

## 바로 다음에 할 일

1. 휴대폰 크기 화면에서 5단계를 다시 한 번 눌러본다.
2. OpenAI API 키를 `.env.local`에 넣고 실제 그림일기 3장을 테스트한다.
3. 손글씨 인식이 틀려도 부모 확인 화면에서 수정하면 이야기에 반영되는지 본다.
4. `VERCEL_DEPLOY_2026-09-16.md`를 따라 배포한다.
5. 배포된 주소를 휴대폰에서 열어 본다.
6. 원티드 제출 폼의 서비스 주소를 배포 주소로 바꾼다.

## 보안과 아이 정보

- 실명 대신 별명을 사용한다.
- 얼굴, 학교명, 주소, 연락처가 보이는 사진은 올리지 않는다.
- API 키는 `.env.local`이나 Vercel 비밀금고에만 넣는다.
- 현재 버전은 사진과 이야기를 데이터베이스에 저장하지 않는다.
- 저장·공유·회원가입을 붙이면 부모 동의, 삭제, 개인정보 처리방침, 비공개 저장소를 함께 구현한다.

## 완료 기준

- 예제 모드로 5단계가 끝까지 진행된다.
- AI 인식 내용을 부모가 수정할 수 있다.
- 한국어·영어·이중언어 보기가 작동한다.
- 이야기는 항상 4페이지다.
- 마지막 화면에서 재생성 피드 대신 종이 그림을 제안한다.
- `npm test`와 `npm run build`가 통과한다.
- Vercel 배포 주소가 심사 기간 동안 열린다.
