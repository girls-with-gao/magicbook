# 그림이야기 집 컴퓨터 작업 인수인계

이 문서는 집 컴퓨터에서 이 프로젝트를 바로 이어하기 위한 안내서다.

## 가장 먼저 읽을 문서

확정된 제품 설계는 다음 파일에 있다.

- `docs/superpowers/specs/2026-09-16-drawtale-bilingual-mvp-design.md`

집 컴퓨터의 Codex에서 다음처럼 말하면 된다.

> HANDOFF_2026-09-16.md와 docs/superpowers/specs/2026-09-16-drawtale-bilingual-mvp-design.md를 먼저 모두 읽고, 현재 코드와 설계의 차이를 확인해줘. 그런 다음 구현 계획부터 만들어줘. 기존 파일은 말없이 덮어쓰지 마.

## GitHub 저장소

- <https://github.com/girls-with-gao/magicbook>
- 기본 브랜치: `main`

## 현재 코드의 상태

현재 저장소에는 작동하는 초기 프로토타입이 있다.

- `server.js`: 웹 서버, OpenAI 호출, 데모 이야기
- `public/index.html`: 업로드와 이야기 화면
- `public/app.js`: 사진 업로드, AI 요청, 페이지 전환
- `public/styles.css`: 화면 디자인
- `README.md`: 현재 프로토타입 실행 안내

현재 프로토타입으로 할 수 있는 것:

- 그림·그림일기 업로드
- 별명·나이·테마 입력
- 완성형·이어쓰기형 선택
- OpenAI를 이용한 그림 분석과 이야기 생성
- 한국어·영어 이야기
- 다음 상상 질문
- API 키 없이 보여주는 데모 모드
- 인쇄 기능을 이용한 PDF 저장

## 이번에 확정한 방향

프로젝트의 이름은 일단 `그림이야기` / `DrawTale`로 사용한다.

핵심 흐름:

1. 부모가 그림일기를 올린다.
2. AI가 그림과 손글씨를 읽는다.
3. 부모가 AI가 읽은 내용을 확인·수정한다.
4. 한국어, 영어, 이중언어 보기 중 하나를 고른다.
5. 아이가 원본 그림으로 만든 4페이지 이야기를 본다.
6. 마지막 질문을 받고 화면을 끄고 다음 장면을 종이에 그린다.

핵심 문장:

> 그림이야기는 아이의 그림일기를 AI가 한국어와 영어의 짧은 이야기로 확장하고, 마지막 질문을 통해 아이가 다시 종이 위에 다음 상상을 그리게 하는 이중언어 창작 서비스다.

## 현재 코드와 목표의 차이

다음은 아직 구현되지 않았다.

1. AI가 읽은 그림·손글씨 내용을 부모가 확인·수정하는 화면
2. 한국어, 영어, 이중언어 세 가지 보기 모드
3. 원본 그림을 페이지별로 확대·이동하는 시각 구성
4. 한 페이지당 학습 단어를 최대 2개로 제한하는 구조
5. 마지막에 종이 활동으로 끝내는 전용 종료 화면
6. 문장별 음성 낭독
7. Next.js App Router 구조와 Vercel 배포

## 다음 작업 순서

폭을 넓히지 말고 다음 순서로 진행한다.

1. 설계서를 기준으로 구현 계획을 작성한다.
2. 현재 프로토타입을 Next.js App Router로 이전한다.
3. 먼저 API 키 없이도 작동하는 데모 흐름 5단계를 완성한다.
4. AI 그림·손글씨 분석과 부모 확인 단계를 붙인다.
5. 확인된 내용으로 4페이지 한국어·영어 이야기를 생성한다.
6. 언어 표시 모드와 오프라인 종료 화면을 완성한다.
7. 휴대폰에서 처음부터 끝까지 시연한다.
8. Vercel에 배포하고 대회 제출용 주소를 확인한다.

## 범위에서 뺀 것

대회 제출 전까지는 다음을 만들지 않는다.

- 회원가입, 로그인, 성장 아카이브
- 실물 책 주문·배송
- 만화·애니메이션·영상 생성
- 점수·미션·출석·무한 스크롤
- 영구 이미지 저장
- 한국어·영어 외 추가 언어

## 보안과 아이 정보

- API 키는 코드나 채팅방에 적지 말고 `.env.local`에만 넣는다.
- `.env.local`은 GitHub에 올리지 않는다.
- 아이 실명 대신 별명을 사용한다.
- 대회 MVP에서는 사진과 이야기를 영구 저장하지 않는다.
- 얼굴, 학교명, 주소, 연락처가 보이는 이미지는 올리지 않도록 안내한다.
- 언젠가 저장·공유·회원가입을 붙이면 부모 동의, 삭제, 개인정보 처리방침, 비공개 저장소를 함께 구현한다.

## 집 컴퓨터에서 실행하는 방법

Node.js 20 이상이 설치되어 있다면 현재 프로토타입을 다음처럼 실행할 수 있다.

```bash
npm run dev
```

인터넷 창에서 <http://localhost:3000>을 연다.

API 키가 없어도 데모 모드는 작동한다. 실제 AI 기능을 쓰려면 저장소의 `.env.example`을 참고해 `.env`를 만들고 자신의 키를 넣는다. 키 값은 GitHub, 카카오톡, 채팅방에 올리지 않는다.

## 중요한 현실 점검

- 제품 설계와 현재 코드는 완전히 같지 않다.
- 현재 코드는 시연 가능한 초기 프로토타입이고, 확정된 5단계 흐름을 구현해야 한다.
- `README.md`는 현재 코드를 설명하고, 설계서는 다음 버전의 목표를 설명한다.
- 현재 서버는 `127.0.0.1`에서 실행되는 Node.js 서버라 Vercel에 그대로 올리는 구조가 아니다. Next.js로 이전한 뒤 Vercel 배포를 진행한다.

## 완료 기준

다음이 모두 되면 대회 제출용 MVP가 완료된 것으로 본다.

- 휴대폰에서 그림일기 업로드부터 종료 화면까지 진행됨
- AI 인식 내용을 부모가 확인·수정할 수 있음
- 한국어·영어·이중언어 보기가 작동함
- 이야기가 4페이지로 제한됨
- 마지막 질문이 종이 그림 활동으로 연결됨
- API 키 없이도 발표용 데모가 작동함
- 배포된 Vercel 주소가 심사 기간 동안 열림
