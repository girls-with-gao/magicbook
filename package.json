{
  "name": "magicbook",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.js",
    "demo": "OPENAI_API_KEY= node server.js",
    "test": "vitest run",
    "video": "node --env-file=.env index.ts",
    "seed-images": "node --env-file=.env scripts/generate-seed-images.ts",
    "seed-art": "node --env-file=.env scripts/generate-seed-art.ts",
    "seed-compose": "node scripts/compose-seed-art.ts"
  },
  "dependencies": {
    "@higgsfield/client": "^0.2.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20.9"
  }
}
